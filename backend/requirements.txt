# ============================================================================
# NutriAgent Backend - Dependencies
# Python 3.12 / FastAPI / PostgreSQL / Redis / LangGraph
# ============================================================================

# --- Web Framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.12

# --- Database ---
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.14,<2.0
pgvector>=0.3,<1.0               # pgvector integration for SQLAlchemy

# --- Redis ---
redis[hiredis]>=5.0,<6.0

# --- Authentication ---
python-jose[cryptography]>=3.3,<4.0
bcrypt>=4.0,<5.0

# --- Data Validation ---
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
email-validator>=2.1,<3.0

# --- AI / LLM ---
langgraph>=0.2,<1.0
langchain>=0.3,<1.0
langchain-openai>=0.2,<1.0
langchain-anthropic>=0.2,<1.0
openai>=1.50,<2.0

# --- Embeddings (local, free, no API key needed) ---
sentence-transformers>=2.7,<3.0

# --- HTTP Client ---
httpx>=0.27,<1.0

# --- Utilities ---
python-dateutil>=2.9,<3.0
tenacity>=9.0,<10.0               # Retry logic
orjson>=3.10,<4.0                 # Fast JSON serialization

# --- Observability ---
structlog>=24.0,<25.0             # Structured logging

# --- Dev / Test ---
pytest>=8.0,<9.0
pytest-asyncio>=0.24,<1.0
httpx  # reuse for TestClient (FastAPI testing)
coverage>=7.0,<8.0
